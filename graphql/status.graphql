# Mutation
extend type Mutation {
    swithcStatus(status_id: Int): StatusChange @field(resolver:"App\\GraphQL\\Mutations\\StatusMutation@statusSwitch") @guard
}

# Types
type StatusChange {
    statusid: ID,
    session: Session
}